/**
 * Centralized image imports for the lingua app.
 *
 * Import every image asset here and use it through the `images` object so
 * screens/components never `require()` assets directly.
 */
import mascotAuth from "@/assets/images/mascot-auth.png";
import mascotLogo from "@/assets/images/moscot-logo.png";
import mascotWelcome from "@/assets/images/mascot-welcome.png";

export const images = {
  mascotAuth,
  mascotLogo,
  mascotWelcome,
};
