import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

// Home screen UI is intentionally not built yet — placeholder for now.
export default function HomeScreen() {
  return <ScreenPlaceholder title="Home" />;
}
